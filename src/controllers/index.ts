/**
 * Contains all Express-Route-Controllers. The functions actually responsible for what gets send back to the user. See [[routes]] for the route definitions.
 * @packageDocumentation
 * @module controllers
 * @preferred
 */

export { default as maps } from './maps';
export { default as users } from './users';